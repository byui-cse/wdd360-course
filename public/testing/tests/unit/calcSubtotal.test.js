import { describe, it } from 'vitest'
import { calcSubtotal } from '../../src/utils/calcSubtotal'

describe('calcSubtotal', () => {
  it.todo('calculates subtotal for a typical multiple-item cart')
  it.todo('returns 0 for an empty cart')
  it.todo('handles an item with zero quantity')
  it.todo('handles decimal prices')

  calcSubtotal
})
