import { describe, it } from 'vitest'
import { isValidPassword } from '../../src/utils/isValidPassword'

describe('isValidPassword', () => {
  it.todo('accepts a valid password')
  it.todo('rejects passwords shorter than 8 chars')
  it.todo('rejects passwords with no uppercase letter')
  it.todo('rejects passwords with no lowercase letter')
  it.todo('rejects passwords with no number')

  isValidPassword
})
